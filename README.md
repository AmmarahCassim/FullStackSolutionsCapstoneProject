# FullStackSolutionsCapstoneProject

  ## to run:
  * npm install
  * npm install express
  * npm install file-upload
  ## if file upload doesnt work
  * npm install express-fileupload
  * npm install -g nodemon
  
  * nodemon app.js
  ## if nodemon doesnt work
  * node app.js
